export const LOCALES = ['en', 'fi', 'de']
export const PORT = 4667

export const TRANSLATE = {
  // Brand voice injected into the AI prompt
  style:
    'Nordic design brand. Tone: minimal, confident, warm. Short sentences. Prefer active voice.',
  // Words that should never be translated (brand names, product names, etc.)
  keep: ['White', 'Aino'],
}
