export default async function handler(req) {
  // Get preferred locale and set server cookie
}

export const config = {
  // Optional: Add additional exclusions beyond the defaults
  // matcher: ['/special-path-to-exclude'],
}
